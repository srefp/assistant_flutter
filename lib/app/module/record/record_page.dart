import 'package:fluent_ui/fluent_ui.dart';

import '../../../component/wrapper/log_view_wrapper.dart';

class RecordPage extends StatelessWidget {
  const RecordPage({super.key});

  @override
  Widget build(BuildContext context) {
    return LogView();
  }
}
